<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <h1>View</h1>
    <ul>
        <li><a href="/">back</a></li>
    </ul>

    <div>
        <% if(carts.length > 0) { %>
        <% carts.forEach(cart => { %>
            <p><%= cart.title %><p>
        <% }) %>
        <% } %>
    </div>

</body>

</html>