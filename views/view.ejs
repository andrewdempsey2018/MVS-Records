<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head.ejs') %>

<body>
    <h1>View</h1>
    <ul>
        <li><a href="/">back</a></li>
    </ul>

    <div>
        <% if(carts.length > 0) { %>
        <% carts.forEach(cart => { %>
            <p><%= cart.title %><p>
            <a class="single" href="/<%= cart._id %>">More details</a>
        <% }) %>
        <% } %>
    </div>

</body>

</html>